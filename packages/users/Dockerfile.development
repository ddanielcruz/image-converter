# Build image
FROM node:16-alpine

# Create app directory
WORKDIR /root/app

# Copy projects and install dependencies
COPY . .
RUN npm ci

# Start the project
CMD ["npm", "run", "dev", "-w", "@image-converter/users"]
